{% extends 'base.html.twig' %}

{# {% block title %}Edit LdapUser{% endblock %} #}

{% block body %}
    <p class="title-table-index">EDITAR USUARIO</p>

    {{ include('ldap_user/_form.html.twig', {'button_label': 'Guardar'}) }}

    <a class="btn btn-primary" href="{{ path('ldap_user_index') }}">Volver al listado de usuarios</a>

    {{ include('ldap_user/_delete_form.html.twig') }}
{% endblock %}
