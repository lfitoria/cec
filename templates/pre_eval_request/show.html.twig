{% extends 'base.html.twig' %}

{% block title %}PreEvalRequest{% endblock %}

{% block body %}
    <h1>PreEvalRequest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pre_eval_request.id }}</td>
            </tr>
            <tr>
                <th>Observations</th>
                <td>{{ pre_eval_request.observations }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ pre_eval_request.date ? pre_eval_request.date|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Current</th>
                <td>{{ pre_eval_request.current ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('pre_eval_request_index') }}">back to list</a>

    {# <a href="{{ path('pre_eval_request_edit', {'id': pre_eval_request.id}) }}">edit</a> #}
    <a href="{{ path('pre_eval_request_edit', {'id': pre_eval_request.id,'id_request': pre_eval_request.request.id}) }}">edit</a>

    {{ include('pre_eval_request/_delete_form.html.twig') }}
{% endblock %}
