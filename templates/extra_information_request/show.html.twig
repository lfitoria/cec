{% extends 'base.html.twig' %}

{% block title %}ExtraInformationRequest{% endblock %}

{% block body %}
    <h1>ExtraInformationRequest</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ extra_information_request.id }}</td>
            </tr>
            <tr>
                <th>TutorName</th>
                <td>{{ extra_information_request.tutorName }}</td>
            </tr>
            <tr>
                <th>TutorId</th>
                <td>{{ extra_information_request.tutorId }}</td>
            </tr>
            <tr>
                <th>TutorEmail</th>
                <td>{{ extra_information_request.tutorEmail }}</td>
            </tr>
            <tr>
                <th>GrupalProject</th>
                <td>{{ extra_information_request.grupalProject ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('extra_information_request_index') }}">back to list</a>

    <a href="{{ path('extra_information_request_edit', {'id': extra_information_request.id}) }}">edit</a>

    {{ include('extra_information_request/_delete_form.html.twig') }}
{% endblock %}
