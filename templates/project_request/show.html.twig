{% extends 'base.html.twig' %}

{% block title %}ProjectRequest{% endblock %}

{% block body %}
    <h1>Información</h1>

    {{ include('project_request/details.html.twig') }}

    <a href="{{ path('project_request_index') }}">back to list</a>

    <a href="{{ path('project_request_edit', {'id': project_request.id}) }}">edit</a>

    {{ include('project_request/_delete_form.html.twig') }}
{% endblock %}
